# Moltbot 工具大全 🦞

> 🦞 **Moltbot**（蜕皮龙虾）— 强大的个人 AI 助手，通过丰富的工具集实现系统级自动化

---

## 关于本文档

本文档来自 **Moltbot 中文汉化版** 项目。

| 资源 | 链接 |
|------|------|
| **汉化版仓库** | https://github.com/Coldplay-now/clawdbot-cn |
| **Moltbot 官网** | https://molt.bot |
| **官方文档** | https://docs.molt.bot |

---

## 目录

1. [工具概览](#工具概览)
2. [文件系统工具](#文件系统工具)
3. [命令执行工具](#命令执行工具)
4. [网络工具](#网络工具)
5. [浏览器控制工具](#浏览器控制工具)
6. [消息通道工具](#消息通道工具)
7. [会话管理工具](#会话管理工具)
8. [记忆系统工具](#记忆系统工具)
9. [定时任务工具](#定时任务工具)
10. [节点控制工具](#节点控制工具)
11. [其他工具](#其他工具)
12. [工具分组](#工具分组)
13. [工具配置](#工具配置)
14. [安全策略](#安全策略)

---

## 工具概览

Moltbot 提供了一套完整的工具集，让 AI 助手能够：

```
┌─────────────────────────────────────────────────────────────┐
│                    🦞 Moltbot 工具能力                       │
├─────────────────────────────────────────────────────────────┤
│  📁 文件操作     │  读取、写入、编辑文件                      │
│  💻 命令执行     │  运行 Shell 命令                          │
│  🌐 网络访问     │  搜索互联网、抓取网页                      │
│  🖥️ 浏览器控制   │  自动化浏览器操作                         │
│  💬 消息发送     │  多平台消息（TG/Discord/WhatsApp等）       │
│  🧠 记忆管理     │  搜索和获取历史记忆                        │
│  ⏰ 定时任务     │  创建和管理 Cron 任务                      │
│  📱 节点控制     │  控制多设备（拍照、录屏、通知等）          │
└─────────────────────────────────────────────────────────────┘
```

### 工具数量统计

| 类别 | 工具数 | 主要功能 |
|------|--------|---------|
| 文件系统 | 4 | read, write, edit, apply_patch |
| 命令执行 | 2 | exec, process |
| 网络 | 2 | web_search, web_fetch |
| 浏览器 | 1 (多动作) | browser (15+ 动作) |
| 消息 | 1 (多动作) | message (20+ 动作) |
| 会话 | 5 | sessions_*, session_status |
| 记忆 | 2 | memory_search, memory_get |
| 定时 | 1 (多动作) | cron (7 动作) |
| 节点 | 1 (多动作) | nodes (10+ 动作) |
| 其他 | 4 | image, canvas, gateway, agents_list |

---

## 文件系统工具

### `read` - 读取文件

读取文件内容。

```json
{
  "tool": "read",
  "path": "/path/to/file.txt"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `path` | string | ✅ | 文件路径 |

### `write` - 写入文件

创建或覆盖文件。

```json
{
  "tool": "write",
  "path": "/path/to/file.txt",
  "content": "Hello, World!"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `path` | string | ✅ | 文件路径 |
| `content` | string | ✅ | 文件内容 |

### `edit` - 编辑文件

修改现有文件的部分内容。

```json
{
  "tool": "edit",
  "path": "/path/to/file.txt",
  "oldText": "old content",
  "newText": "new content"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `path` | string | ✅ | 文件路径 |
| `oldText` | string | ✅ | 要替换的内容 |
| `newText` | string | ✅ | 替换后的内容 |

### `apply_patch` - 应用补丁

批量修改多个文件（实验性功能）。

```json
{
  "tool": "apply_patch",
  "patches": [
    {
      "path": "/path/to/file1.txt",
      "hunks": [...]
    }
  ]
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `patches` | array | ✅ | 补丁数组 |

> ⚠️ 需要启用：`tools.exec.applyPatch.enabled: true`

---

## 命令执行工具

### `exec` - 执行命令

在工作区运行 Shell 命令。

```json
{
  "tool": "exec",
  "command": "ls -la"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `command` | string | ✅ | 要执行的命令 |
| `yieldMs` | number | - | 自动后台超时（默认 10000ms） |
| `background` | boolean | - | 立即后台运行 |
| `timeout` | number | - | 超时秒数（默认 1800） |
| `elevated` | boolean | - | 提权执行 |
| `host` | string | - | 执行位置：`sandbox`/`gateway`/`node` |
| `security` | string | - | 安全模式：`deny`/`allowlist`/`full` |
| `ask` | string | - | 询问策略：`off`/`on-miss`/`always` |
| `pty` | boolean | - | 使用 PTY 终端 |

**返回值**：
- 同步：命令输出 + 退出码
- 后台：`status: "running"` + `sessionId`

### `process` - 进程管理

管理后台执行的命令。

| 动作 | 说明 |
|------|------|
| `list` | 列出所有后台进程 |
| `poll` | 获取新输出和状态 |
| `log` | 获取完整日志（支持 offset/limit） |
| `write` | 向进程写入输入 |
| `kill` | 终止进程 |
| `clear` | 清理已完成进程 |
| `remove` | 移除进程记录 |

```json
{
  "tool": "process",
  "action": "poll",
  "sessionId": "xxx-xxx-xxx"
}
```

---

## 网络工具

### `web_search` - 网络搜索

使用 Brave Search API 搜索互联网。

```json
{
  "tool": "web_search",
  "query": "Moltbot AI assistant"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `query` | string | ✅ | 搜索关键词 |
| `count` | number | - | 结果数量（1-10） |
| `country` | string | - | 国家代码（如 "CN", "US"） |
| `freshness` | string | - | 时间过滤：`pd`/`pw`/`pm`/`py` |

**时间过滤值**：
- `pd`: 过去 24 小时
- `pw`: 过去一周
- `pm`: 过去一月
- `py`: 过去一年

> ⚠️ 需要配置 `BRAVE_API_KEY` 或 `tools.web.search.apiKey`

### `web_fetch` - 网页抓取

获取 URL 内容并转换为可读格式。

```json
{
  "tool": "web_fetch",
  "url": "https://example.com"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `url` | string | ✅ | 目标 URL |
| `extractMode` | string | - | `markdown` 或 `text` |
| `maxChars` | number | - | 最大字符数 |

> 💡 JS 密集型网站建议使用 `browser` 工具

---

## 浏览器控制工具

### `browser` - 浏览器自动化

控制 Chrome/Brave/Edge 浏览器，实现网页自动化。

#### 基础操作

| 动作 | 说明 | 参数 |
|------|------|------|
| `status` | 浏览器状态 | - |
| `start` | 启动浏览器 | `profile?` |
| `stop` | 停止浏览器 | `profile?` |
| `tabs` | 列出标签页 | - |
| `open` | 打开 URL | `url` |
| `focus` | 聚焦标签 | `tabId` |
| `close` | 关闭标签 | `tabId` |

#### 页面操作

| 动作 | 说明 | 参数 |
|------|------|------|
| `snapshot` | 页面快照 | `mode: "ai"/"aria"` |
| `screenshot` | 截图 | - |
| `navigate` | 导航 | `url`, `action: "back"/"forward"/"reload"` |
| `pdf` | 导出 PDF | `path` |
| `console` | 控制台日志 | - |

#### 交互操作 (`act`)

```json
{
  "tool": "browser",
  "action": "act",
  "ref": "12",
  "type": "click"
}
```

| 交互类型 | 说明 |
|----------|------|
| `click` | 点击元素 |
| `type` | 输入文字 |
| `press` | 按键 |
| `hover` | 悬停 |
| `drag` | 拖拽 |
| `select` | 选择下拉项 |
| `fill` | 填充表单 |
| `wait` | 等待 |
| `evaluate` | 执行 JS |

#### Profile 管理

| 动作 | 说明 |
|------|------|
| `profiles` | 列出所有 Profile |
| `create-profile` | 创建新 Profile |
| `delete-profile` | 删除 Profile |
| `reset-profile` | 重置 Profile |

**典型工作流**：
```
1. browser → status / start
2. snapshot (获取页面结构)
3. act (点击/输入)
4. screenshot (确认结果)
```

---

## 消息通道工具

### `message` - 多平台消息

跨平台发送和管理消息。

**支持的平台**：
- 📱 WhatsApp
- ✈️ Telegram
- 🎮 Discord
- 💼 Slack
- 📡 Signal
- 💬 iMessage
- 🏢 Google Chat
- 👔 MS Teams

#### 消息操作

| 动作 | 说明 | 主要参数 |
|------|------|---------|
| `send` | 发送消息 | `channel`, `target`, `text`, `media?` |
| `react` | 添加表情 | `messageId`, `emoji` |
| `reactions` | 查看表情 | `messageId` |
| `read` | 标记已读 | `messageId` |
| `edit` | 编辑消息 | `messageId`, `text` |
| `delete` | 删除消息 | `messageId` |

#### 高级操作

| 动作 | 说明 |
|------|------|
| `poll` | 创建投票 |
| `pin` / `unpin` | 置顶/取消置顶 |
| `list-pins` | 列出置顶消息 |
| `search` | 搜索消息 |
| `sticker` | 发送贴纸 |

#### 频道/群组操作

| 动作 | 说明 |
|------|------|
| `thread-create` | 创建话题 |
| `thread-list` | 列出话题 |
| `thread-reply` | 话题回复 |
| `channel-info` | 频道信息 |
| `channel-list` | 列出频道 |

#### 用户/角色操作

| 动作 | 说明 |
|------|------|
| `member-info` | 成员信息 |
| `role-info` | 角色信息 |
| `role-add` / `role-remove` | 添加/移除角色 |
| `timeout` / `kick` / `ban` | 禁言/踢出/封禁 |

#### 其他操作

| 动作 | 说明 |
|------|------|
| `voice-status` | 语音状态 |
| `event-list` | 活动列表 |
| `event-create` | 创建活动 |
| `emoji-list` | 表情列表 |
| `emoji-upload` | 上传表情 |
| `sticker-upload` | 上传贴纸 |
| `permissions` | 权限信息 |

**发送消息示例**：
```json
{
  "tool": "message",
  "action": "send",
  "channel": "telegram",
  "target": "123456789",
  "text": "Hello from Moltbot! 🦞"
}
```

---

## 会话管理工具

### `sessions_list` - 列出会话

查看所有活跃会话。

```json
{
  "tool": "sessions_list",
  "kinds": ["dm", "group"],
  "limit": 10,
  "activeMinutes": 60
}
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `kinds` | array | 会话类型过滤 |
| `limit` | number | 最大数量 |
| `activeMinutes` | number | 活跃时间范围 |
| `messageLimit` | number | 每会话消息数 |

### `sessions_history` - 会话历史

获取会话的对话记录。

```json
{
  "tool": "sessions_history",
  "sessionKey": "main",
  "limit": 50,
  "includeTools": false
}
```

### `sessions_send` - 跨会话发送

向另一个会话发送消息。

```json
{
  "tool": "sessions_send",
  "sessionKey": "support",
  "message": "需要帮助",
  "timeoutSeconds": 30
}
```

### `sessions_spawn` - 启动子 Agent

启动一个子 Agent 执行任务。

```json
{
  "tool": "sessions_spawn",
  "task": "帮我分析这份报告",
  "label": "报告分析",
  "agentId": "analyst"
}
```

### `session_status` - 会话状态

获取当前会话状态。

```json
{
  "tool": "session_status",
  "sessionKey": "main"
}
```

---

## 记忆系统工具

Moltbot 拥有持久记忆系统，可以记住用户偏好和历史信息。

### `memory_search` - 搜索记忆

在记忆库中搜索相关信息。

```json
{
  "tool": "memory_search",
  "query": "用户的编程语言偏好"
}
```

### `memory_get` - 获取记忆

获取特定的记忆条目。

```json
{
  "tool": "memory_get",
  "key": "user_preferences"
}
```

---

## 定时任务工具

### `cron` - 定时任务管理

管理 Gateway 的定时任务。

| 动作 | 说明 |
|------|------|
| `status` | Cron 服务状态 |
| `list` | 列出所有任务 |
| `add` | 添加新任务 |
| `update` | 更新任务 |
| `remove` | 删除任务 |
| `run` | 立即执行任务 |
| `runs` | 查看执行历史 |
| `wake` | 唤醒 Agent |

**添加定时任务示例**：
```json
{
  "tool": "cron",
  "action": "add",
  "job": {
    "name": "daily-summary",
    "description": "每日总结",
    "schedule": {
      "type": "every",
      "every": 1,
      "unit": "days",
      "at": "09:00"
    },
    "payload": {
      "type": "agent-turn",
      "message": "给我今日待办事项总结"
    }
  }
}
```

---

## 节点控制工具

### `nodes` - 多设备管理

发现和控制配对的设备节点。

#### 基础操作

| 动作 | 说明 |
|------|------|
| `status` | 查看所有节点状态 |
| `describe` | 获取节点详细信息 |

#### 配对管理

| 动作 | 说明 |
|------|------|
| `pending` | 待配对请求 |
| `approve` | 批准配对 |
| `reject` | 拒绝配对 |

#### 通知和执行

| 动作 | 说明 | 参数 |
|------|------|------|
| `notify` | 发送系统通知 | `title`, `body` |
| `run` | 远程执行命令 | `command`, `cwd?`, `env?` |

**远程执行示例**：
```json
{
  "tool": "nodes",
  "action": "run",
  "node": "office-mac",
  "command": ["echo", "Hello"],
  "env": ["FOO=bar"],
  "commandTimeoutMs": 12000
}
```

#### 媒体捕获

| 动作 | 说明 | 返回 |
|------|------|------|
| `camera_snap` | 拍照 | 图片 |
| `camera_clip` | 录制视频片段 | MP4 文件 |
| `screen_record` | 屏幕录制 | MP4 文件 |

#### 位置

| 动作 | 说明 | 返回 |
|------|------|------|
| `location_get` | 获取位置 | JSON (lat/lon/accuracy) |

> ⚠️ 媒体捕获需要节点 App 在前台运行

---

## 其他工具

### `image` - 图像分析

使用视觉模型分析图片。

```json
{
  "tool": "image",
  "image": "/path/to/image.png",
  "prompt": "描述这张图片的内容"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `image` | string | ✅ | 图片路径或 URL |
| `prompt` | string | - | 分析提示（默认"描述图片"） |
| `model` | string | - | 指定模型 |

### `canvas` - Canvas 渲染

驱动节点 Canvas 进行渲染。

| 动作 | 说明 |
|------|------|
| `present` | 展示内容 |
| `hide` | 隐藏 |
| `navigate` | 导航 |
| `eval` | 执行代码 |
| `snapshot` | 截图 |
| `a2ui_push` | 推送 A2UI |
| `a2ui_reset` | 重置 A2UI |

### `gateway` - 网关控制

控制 Gateway 进程。

| 动作 | 说明 |
|------|------|
| `restart` | 重启 Gateway |
| `config.get` | 获取配置 |
| `config.schema` | 配置 Schema |
| `config.apply` | 应用配置 |
| `config.patch` | 部分更新配置 |
| `update.run` | 运行更新 |

> ⚠️ `restart` 需要启用：`commands.restart: true`

### `agents_list` - 列出 Agent

列出可用于 `sessions_spawn` 的 Agent。

```json
{
  "tool": "agents_list"
}
```

---

## 工具分组

在配置中使用 `group:*` 快速引用一组工具：

| 分组 | 包含工具 |
|------|---------|
| `group:fs` | `read`, `write`, `edit`, `apply_patch` |
| `group:runtime` | `exec`, `bash`, `process` |
| `group:web` | `web_search`, `web_fetch` |
| `group:ui` | `browser`, `canvas` |
| `group:sessions` | `sessions_list`, `sessions_history`, `sessions_send`, `sessions_spawn`, `session_status` |
| `group:memory` | `memory_search`, `memory_get` |
| `group:messaging` | `message` |
| `group:automation` | `cron`, `gateway` |
| `group:nodes` | `nodes` |
| `group:clawdbot` | 所有内置工具 |

**使用示例**：
```json5
{
  "tools": {
    "allow": ["group:fs", "group:web"],  // 只允许文件和网络工具
    "deny": ["group:runtime"]            // 禁止命令执行
  }
}
```

---

## 工具配置

### 工具 Profile（预设）

| Profile | 包含工具 | 适用场景 |
|---------|---------|---------|
| `minimal` | 仅 `session_status` | 最小权限 |
| `coding` | 文件、运行时、会话、记忆、图像 | 编程助手 |
| `messaging` | 消息、会话 | 消息机器人 |
| `full` | 所有工具 | 完整功能 |

**配置示例**：
```json5
{
  "tools": {
    "profile": "coding",
    "deny": ["exec"]  // 进一步限制
  }
}
```

### 按 Provider 配置

为不同的 LLM Provider 设置不同的工具权限：

```json5
{
  "tools": {
    "profile": "coding",
    "byProvider": {
      "deepseek": { "profile": "minimal" },
      "anthropic/claude-sonnet": { "allow": ["group:fs", "web_search"] }
    }
  }
}
```

---

## 安全策略

### exec 工具安全

| 配置项 | 值 | 说明 |
|--------|---|------|
| `security` | `deny` | 拒绝所有命令 |
| | `allowlist` | 仅白名单命令 |
| | `full` | 允许所有命令 |
| `ask` | `off` | 不询问 |
| | `on-miss` | 白名单外询问 |
| | `always` | 总是询问 |

**推荐安全配置**：
```json5
// ~/.clawdbot/exec-approvals.json
{
  "defaults": {
    "security": "deny",
    "ask": "always",
    "askFallback": "deny"
  },
  "agents": {
    "main": {
      "security": "allowlist",
      "allowlist": [
        { "pattern": "/usr/bin/head" },
        { "pattern": "/usr/bin/tail" },
        { "pattern": "/usr/bin/grep" },
        { "pattern": "/usr/bin/cat" },
        { "pattern": "/bin/ls" }
      ]
    }
  }
}
```

### 工具禁用

全局禁用危险工具：
```json5
{
  "tools": {
    "deny": ["exec", "process", "browser"]
  }
}
```

---

## 快速参考卡片

```
┌────────────────────────────────────────────────────────────────┐
│                    🦞 Moltbot 工具速查                          │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  📁 文件        read / write / edit / apply_patch              │
│  💻 命令        exec / process                                 │
│  🌐 网络        web_search / web_fetch                         │
│  🖥️ 浏览器      browser (start/snapshot/act/screenshot...)    │
│  💬 消息        message (send/react/edit/delete/poll...)      │
│  🗂️ 会话        sessions_list / sessions_history / ...        │
│  🧠 记忆        memory_search / memory_get                     │
│  ⏰ 定时        cron (list/add/update/remove/run/wake)        │
│  📱 节点        nodes (status/notify/run/camera/screen...)    │
│  🖼️ 图像        image                                         │
│  🎨 Canvas      canvas                                         │
│  ⚙️ 网关        gateway                                        │
│                                                                │
├────────────────────────────────────────────────────────────────┤
│  配置文件: ~/.clawdbot/clawdbot.json                           │
│  审批配置: ~/.clawdbot/exec-approvals.json                     │
│  查看工具: clawdbot rpc tools.list                             │
└────────────────────────────────────────────────────────────────┘
```

---

## 参考链接

- [Moltbot 官方文档](https://docs.molt.bot)
- [工具文档](https://docs.molt.bot/tools)
- [浏览器控制](https://docs.molt.bot/tools/browser)
- [Web 工具](https://docs.molt.bot/tools/web)
- [Exec 审批](https://docs.molt.bot/tools/exec-approvals)
- [Brave Search 配置](https://docs.molt.bot/brave-search)

---

**🦞 Happy Automating with Moltbot!**
